# Riwaaz Frontend Backend Integration

Backend: Spring Boot (Java 21)
API Base URL: http://localhost:8080/api

Replace mock authentication with JWT login/register endpoints.
Store JWT in localStorage and send Authorization: Bearer <token>.
